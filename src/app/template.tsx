"use client";

/**
 * Global Route Template
 * ─────────────────────
 * Simplified to remove legacy shutter mask.
 */
export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
